"""Temporal information boundary primitives.

Import concrete types from ``backend.temporal.snapshot`` and
``backend.temporal.audit``. Keeping package initialization side-effect free also
allows the snapshot module to serve as a clean ``python -m`` entry point.
"""
