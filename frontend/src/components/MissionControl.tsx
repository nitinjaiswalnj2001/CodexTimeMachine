import type {DemoBundle} from '../types/demo';
export function MissionControl({bundle,onGuided,onExplore}:{bundle:DemoBundle;onGuided:()=>void;onExplore:()=>void}){const a=bundle.artifacts;return <><header><div><p className="eyebrow">TEMPORAL FORENSICS / ACCEPTED EVIDENCE</p><h1>Git remembers what changed.<br/><em>Codex Time Machine reconstructs why.</em></h1><p className="lede">A forensic system for testing whether an AI engineering decision would change when given the minimum future evidence it originally lacked.</p></div><div className="integrity">LIVE MODEL INVOKED <b>FALSE</b><br/>RECEIPT <b>VALIDATED</b></div></header><nav className="screen-nav"><button onClick={onGuided}>Launch Guided Demo</button><button onClick={onExplore}>Free Explore</button></nav><div className="mission"><div className="glass hero"><p>SCENARIO</p><h2>{String(a.evaluation.scenario_id??'Unavailable in accepted bundle')}</h2><dl><dt>Baseline run</dt><dd>{String(a.trajectory.run_id??'Unavailable in accepted bundle')}</dd><dt>Replay</dt><dd>{String(a.replay_manifest.replay_id??'Unavailable in accepted bundle')}</dd><dt>Coverage shift</dt><dd className="green">{String(a.counterfactual.shift?.status??'Unavailable in accepted bundle')}</dd></dl></div><div className="glass hashes"><p>INTEGRITY RECEIPT</p>{Object.entries(a.phase_8_receipt).filter(([,v])=>typeof v==='string').slice(0,5).map(([k,v])=><div key={k}><small>{k}</small><code>{String(v)}</code></div>)}</div></div></>}
