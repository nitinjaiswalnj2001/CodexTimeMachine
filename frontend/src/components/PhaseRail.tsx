const phases=['Boundary','Isolated Run','Trajectory','Future Context','Blind Spot','Ghost Clue','Replay','Divergence','Target Coverage'];
export function PhaseRail({active,onSelect,reduced,onReduced}:{active:number;onSelect:(value:number)=>void;reduced:boolean;onReduced:()=>void}){return <aside className="rail" aria-label="Backend evidence phases"><div className="brand">CODEX<br/><span>TIME MACHINE</span></div>{phases.map((phase,index)=><button className={active===index?'active':''} onClick={()=>onSelect(index)} key={phase}><i>{index+1}</i>{phase}</button>)}<div className="controls"><button onClick={onReduced}>Reduced Motion: {reduced?'On':'Off'}</button></div></aside>}
